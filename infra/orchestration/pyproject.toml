[project]
name = "orchestration"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Jack", email = "jcm2022@icloud.com" }
]
requires-python = "==3.13.*"
dependencies = [
    "boto3>=1.43.78",
    "prefect>=3.8.3",
    "python-terraform>=0.10.1",
    # orchestrator.py POSTs a repository_dispatch to GitHub. Already present
    # transitively via prefect; declared because we import it directly.
    "requests>=2.32.0",
    "common",
    "polars>=1.43.2",
]

[project.scripts]
orchestration = "orchestration.orchestrator:cli_run"

[tool.uv.sources]
common = {path = "../../libs/common", editable = true }

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
